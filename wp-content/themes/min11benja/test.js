// Input Dictionario: Horse 
//output : Shore, seroh, etc


dic = get_dictionary();
next = next_word(i);

void getAnagremas(String Ref) {



        var aLongitud = Ref.size();
        var palabra_original = Ref;
        var palabras_reordenadas[aLongitud][aLongitud];
        var palabras_dic = get_dictionary();
        var iContCantPalabras=0;

        //guardar dentro de distinto areglo distintas versiones de la palabra original iterando el orden de indice de cada areglo 
        for (int i = 0; i < aLongitud; i++) {
            for (int j = 0; j < aLongitud; j++) {
                palabras_reordenadas[j][i] = palabra_original[i + 1];
            }
            iContCantPalabras=iContCantPalabras+1;
        }

        //comparar la palabra reordenada con la palabra obtenida de la funcion get_dictionary() si es igual cambiar variable booleana para salir del cilo while y regresar la palabra 
        
        for(int k=0;k<){
            
        }
        if () {
            
            

        } else {

        }




}
